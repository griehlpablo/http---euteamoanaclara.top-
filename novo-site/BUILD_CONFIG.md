# Configuração de build

O build público usa a configuração padrão do Supabase definida no código do site. Isso mantém a página da Milka e os lembretes conectados ao mesmo projeto e ao bucket `mural`.
